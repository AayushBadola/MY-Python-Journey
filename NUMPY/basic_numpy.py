import numpy as np # using numpy with an alias np , its a multi dimensional array library

# a numpy array is superior to a python list 
# it will also give us many operations and also is much faster 

array = np.array([1,2,3,4]) # creates a numpy array using np.array 
print(array)

# checking if the type is NUMPY ARRAY 
print(type(array)) # provides a class numpy.nd array that is multi dimensional array

